{{ include "hello-world-springbootapp.fullname" . }} has been deployed.

{{- if .Values.route.enabled }}

To access the application via its OpenShift Route, run:

  oc get route {{ include "hello-world-springbootapp.fullname" . }} -o jsonpath='{.spec.host}{"\n"}'

Then test the endpoint:

  curl -k "https://$(oc get route {{ include "hello-world-springbootapp.fullname" . }} -o jsonpath='{.spec.host}')/hello?name=World"
{{- else }}

Route is disabled. To access the service, port-forward locally:

  oc port-forward svc/{{ include "hello-world-springbootapp.fullname" . }} 8080:8080

Then visit http://localhost:8080/hello?name=World
{{- end }}
